version: 2
jobs:
  build:
    docker:
    - image: alpine:3.6.5
    steps:
    - checkout
    - run:
        name: Echo
        command: echo "hello, world."

workflows:
  version: 2
  first_one:
    jobs:
    - build
