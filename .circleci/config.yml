version: 2
jobs:
  shellcheck:
    docker:
      - image: koalaman/shellcheck-alpine:stable
    steps:
      - checkout
      - run:
          name: Check Scripts
          command: |
            echo "hiell yar"
            ls -ltr
workflows:
  version: 2
  workflow:
    jobs:
      - shellcheck

